# Spec 012: README Update

## Problem
README is stale — reports 49 tests (now 90), doesn't document memory system, health monitoring, or watchdog.

## Solution
Update README with current feature set: three-tier memory, log rotation, heartbeat, watchdog, circuit breaker. Fix test count and project structure listing.
